```preview
<HiddenVisually>Available only to screen readers</HiddenVisually>
```

<Highlight>Hides content while keeping screen readers announcements</Highlight>

<br />
<br />

## Usage

HiddenVisually will hide everything passed as `children` from the screen but will still read that content when navigating with a screen reader.
This way, you can add extra information for screen reader users.

```code
<HiddenVisually>Available only to screen readers</HiddenVisually>
```
