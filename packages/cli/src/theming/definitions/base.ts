import { BaseThemeDefinition } from "../tokens/types";

const base: BaseThemeDefinition = {
	viewport: {
		s: { maxPx: 660 },
		m: { minPx: 661 },
		l: { minPx: 900 },
		xl: { minPx: 1280 },
	},
};

export default base;
