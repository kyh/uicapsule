{
  "name": "design-capsule-chrome-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf dist .cache",
    "build:tailwind": "tailwindcss build src/style.css -c ./tailwind.config.js -o dist/style.css",
    "dev": "NODE_ENV=development npm run build:tailwind && cp src/manifest.json dist/ && cp -R src/public dist/ && parcel watch --no-hmr src/{background.js,index.js}",
    "build": "NODE_ENV=production npm run build:tailwind && cp src/manifest.json dist/ && cp -R src/public dist/ && parcel build src/{background.js,index.js}",
    "zip": "zip -r chrome-extension.zip ./dist"
  },
  "dependencies": {
    "jquery": "^3.4.1",
    "crx-hotreload": "^1.0.4",
    "tailwindcss": "^1.4.6"
  },
  "devDependencies": {
    "autoprefixer": "^9.8.3",
    "parcel": "^2.0.0-beta.1",
    "postcss": "^8.2.4",
    "postcss-import": "^12.0.1"
  }
}
