{
  "name": "ui-capsule-chrome-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf dist .cache",
    "dev": "NODE_ENV=development cp src/manifest.json dist/ && cp -R src/public dist/ && parcel watch --no-hmr src/{background.js,index.js}",
    "build": "NODE_ENV=production cp src/manifest.json dist/ && cp -R src/public dist/ && parcel build src/{background.js,index.js}",
    "zip": "zip -r chrome-extension.zip ./dist"
  },
  "dependencies": {
    "crx-hotreload": "^1.0.4",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-redux": "^7.2.2",
    "redux": "^4.0.5",
    "redux-logger": "^3.0.6",
    "styled-components": "^5.2.1"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  }
}
