{
  "name": "ui-capsule",
  "workspaces": [
    "extension",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "dev:extension": "npm run dev --workspace=extension",
    "postinstall": "husky install"
  },
  "devDependencies": {
    "husky": "^6.0.0",
    "prettier": "^2.2.1",
    "pretty-quick": "^3.1.0"
  }
}
